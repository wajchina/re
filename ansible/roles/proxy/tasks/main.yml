---

- name: Import assert.yml
  ansible.builtin.import_tasks: assert.yml
  run_once: yes
  delegate_to: localhost
  tags:
    - squid

- name: Build squid server by ubuntu/squid
  ansible.builtin.import_tasks: squid.yml
  tags:
    - squid
