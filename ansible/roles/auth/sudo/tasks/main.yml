---

- name: Configure /etc/sudoers.d/re
  ansible.builtin.template:
    src: sudoers.j2
    dest: /etc/sudoers.d/re
    mode: '0440'
    validate: /usr/sbin/visudo -cf %s
    backup: yes
  when: sudoers is defined
  tags:
    - sudoers
