---

- name: Restart autofs
  ansible.builtin.service:
    name: "{{ autofs_service }}"
    state: restarted
  tags: autofs

- name: Reload autofs
  ansible.builtin.service:
    name: "{{ autofs_service }}"
    state: reloaded
  tags: autofs